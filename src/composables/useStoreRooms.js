import { api_request } from '@/api/request_provider';
import { useStoreRoomsStore } from '@/stores/StoreRoomsStore';
export function useStoreRooms() { const storeRoomsStore = useStoreRoomsStore(); const fetchStoreRooms = (params = {}) => api_request.get('/store-rooms', { params: { max: storeRoomsStore.filters.max, page: storeRoomsStore.filters.page, ...params }, loading: true, loadingProp: 'isLoading', storeName: 'storeRoomsStore', toStore: true, stateProp: 'itemsList', notNotify: true }); const fetchStoreRoom = (id) => api_request.get(`/store-rooms/${id}`, { loading: true, loadingProp: 'isLoading', storeName: 'storeRoomsStore', toStore: true, stateProp: 'itemData', notNotify: true }); const createStoreRoom = (data) => api_request.post('/store-rooms', { data, loading: true, loadingProp: 'isSaving', storeName: 'storeRoomsStore', notify: true, resultMessage: 'Store room created successfully' }); const updateStoreRoom = (id, data) => api_request.put(`/store-rooms/${id}`, { data, loading: true, loadingProp: 'isSaving', storeName: 'storeRoomsStore', notify: true, resultMessage: 'Store room updated successfully' }); const deleteStoreRoom = (id) => api_request.delete(`/store-rooms/${id}`, { loading: true, loadingProp: 'isLoading', storeName: 'storeRoomsStore', notify: true, resultMessage: 'Store room deleted successfully' }); return { storeRoomsStore, fetchStoreRooms, fetchStoreRoom, createStoreRoom, updateStoreRoom, deleteStoreRoom }; }
