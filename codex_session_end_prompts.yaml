1) We are about to restart the Codex session.

  Create a minimal but sufficient context snapshot to resume work in a new session.
  Include only:
  - project stack
  - key architecture rules
  - current task status
  - files already modified
  - unresolved issues
  - next actionable step

  Exclude redundant discussion history.
  Format it as a continuation briefing.
  Update a file called SESSION_CONTEXT.md with this summary.

==================================================
2)  At New Session:
  Read docs/session-collaboration-rules.md, docs/new-session-handoff.md,
  docs/migration-progress.md, docs/migration-todos.md and others.
  Briefly confirm the current rules, SESSION_CONTEXT.md, keeping the mode: no code/diff output,
  one step at a time, and after each step provide the result and the path to the
  newly created file in plain text.

3) в папку vue2_project были внесены изменения, проанализируй эти изменения и внеси эти зменения в 
  уже мигрированные файлы. если некоторые изменения касаются еще не мигрированных файлов то пропусти это, только если это 
  не файлы из constants, mixins или helpers. Выполни миграцию этих изменений без остановок на
  подтвеждения

4) мигрируй весь раздел без моих подтверждений. не забывай
  использовать уже мигрированные разделы в качестве примера.
  так же используй существующие composable

3) codex resume 019edb20-71cc-7833-8b7b-4335dac6780f