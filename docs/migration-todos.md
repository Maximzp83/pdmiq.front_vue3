# Migration TODOs / Follow-ups

- Process mode for current project phase:
  - One file per step.
  - Apply only after explicit user confirmation.
  - Ask first on any risk/ambiguity.
  - Do not print code or diff in responses unless a risk or ambiguity must be discussed.
  - After each completed step: report result and file path in plain text, then STOP.

- DONE (chunk C-001): migrated `src/views/Plants/LocationItem.vue`, `src/views/Plants/WorkStationItem.vue`, `src/views/Plants/ProcessItem.vue` to `useSubItem`.
- DONE (chunk C-002): added `src/views/Plants/ItemsList.vue` (Vue3 + `useItemsData`).
- DONE (chunk C-003): migrated components `PageMockImg`, `PaginationContainer`, `InfoItem` from `vue2_project/src/components` into `src/components`.
- DONE (chunk C-004): sync-audited all already migrated `src/components/**` files against updated `vue2_project/src/components/**`; applied functional updates in `SearchBar`, `CustomSelect`, `CustomInput`, `Datepicker`.
- DONE (chunk C-005): added temporary stubs `src/views/Plants/Details/DetailsPage.vue` and `src/views/Plants/ItemPage.vue` to fix router import resolution error.
- DONE (chunk C-006): migrated `Filterbar`, `DropdownFilterbar`, `RadioButtonsBlock` from `vue2_project/src/components`.
- DONE (file-by-file): migrated `src/components/gridTable/GridItemCardHeader.vue`.
- DONE (file-by-file): migrated `src/components/table/CustomDataListTable.vue`.
- DONE (file-by-file): migrated `src/components/gridTable/ItemsGridContainer.vue`.
- DONE (file-by-file): migrated `src/components/itemDetails/CounterItem.vue`.
- DONE (file-by-file): migrated `src/components/form/uploadBlock/FileUploadBlockItem.vue`.
- DONE (file-by-file): migrated `src/components/form/uploadBlock/FileUploadBlock.vue`.
- DONE (file-by-file): migrated `src/components/itemDetails/ItemInfoBlock.vue`.
- DONE (file-by-file): migrated `src/components/itemDetails/ItemImagesBlock.vue`.
- DONE (file-by-file): migrated `src/components/itemDetails/ItemPDMsStatisticBlock.vue`.
- DONE (batch): migrated remaining table components `TableHeader`, `Row`, `TableCell`, `TableHeaderCell`; synced table events to `handleEvent`.
- DONE (dependency): migrated `src/components/common/DynamicComponentWrapper.vue` for table component chain.
- DONE (batch): migrated all remaining files from `vue2_project/src/components/common`.
- DONE (batch): migrated remaining required files from `vue2_project/src/components/form` (excluding `SelectorBlock.vue` and `IsolatedSelect.vue` by request).
- DONE (batch): migrated remaining files from `vue2_project/src/components/pages` (`NotFoundPage.vue`, `RemoteLogin.vue`).
- DONE (batch): completed remaining component from `vue2_project/src/components/gridTable` (`CardDroppedSection.vue`).
- DONE (batch): completed remaining components from `vue2_project/src/components/itemDetails`.
- DONE (single-file): finished `src/components/layout/TopNavbar.vue` cleanup/migration alignment.
- DONE (single-file): added `src/components/charts/ChartWrapper.vue` as the base Highcharts wrapper required by pending chart-component migration.
- DONE (single-file): migrated `src/views/MaintenanceCategories/ItemsList.vue` without chart dependencies.
- DONE (single-file): updated `src/composables/mixins/useItemsData.js` to support callback-based list actions and promise-style async flow.
- DONE (coupled step): moved `Plants/ItemsList` create/edit/delete list actions into `src/composables/mixins/useItemsData.js` and rewired the view to consume composable methods.
- DONE (single-file): aligned `src/composables/mixins/useItemsData.js` to use top-level `itemRoute` for create/edit and `apiRoute` for delete; removed `resolveRoutePath`.
- DONE (coupled step): aligned `handleDeleteItems -> deleteItem` to legacy `ids` flow and passed `itemsTableRef` from `src/views/Plants/ItemsList.vue` into `useItemsData`.
- DONE (coupled step): aligned create flow to legacy `/new` pattern by handling `route.params.id === 'new'` in `src/views/Plants/ItemPage.vue` and keeping `itemData` empty without fetch.
- DONE (single-file): restored `useItemsData` filters watcher after switching entity filter updates to store `set_value`.
- DONE (coupled step): aligned `src/views/Applications/ItemsList.vue` and `src/views/Applications/ItemPage.vue` to the `Plants` reference pattern.
- DONE (single-file): updated `src/router/index.js` so migrated entities use explicit `/new` create routes instead of relying on `:id` edit routes.
- DONE (coupled step): aligned `src/views/Processes/ItemsList.vue` and `src/views/Processes/ItemPage.vue` to the `Plants` reference pattern; exposed `selectedIds` from `src/components/gridTable/ItemsGridContainer.vue` for composable-driven delete flow.
- DONE (single-file): updated `src/views/Plants/ItemPage.vue` to use `src/composables/mixins/useItemPage.js`.
- DONE (coupled step): merged `src/composables/mixins/useInitPageData.js` into `src/composables/mixins/useItemPage.js`, removed `useInitPageData.js`, and cleaned up `src/views/Plants/ItemPage.vue`.
- DONE (coupled step): moved generic `fetchItem` and `saveItem` behavior into `src/composables/mixins/useItemPage.js` and removed local request wrappers from `src/views/Plants/ItemPage.vue`.
- DONE (coupled step): updated `src/views/Applications/ItemPage.vue` and `src/views/Machines/ItemPage.vue` to use shared `src/composables/mixins/useItemPage.js`.
- DONE (single-file): updated `src/views/Processes/ItemPage.vue` to use shared `src/composables/mixins/useItemPage.js`.
- DONE (batch): migrated missing `src/views/Plants` files and replaced `ItemPage.vue` stub.
- DONE (single-file): migrated `src/views/Plants/ItemsList.vue` to full list page behavior.
- DONE (single-file): modernized `src/components/common/PaginationContainer.vue` to remove ElPagination deprecated usage.
- DONE (sync): applied relevant updates from latest `vue2_project` changes to already migrated files:
  `src/components/table/CustomDataListTable.vue`, `src/components/table/Row.vue`,
  `src/components/table/TableHeader.vue`, `src/views/Machines/ItemForm.vue`.
- DONE (entity): migrated `Applications` views (`ItemsList`, `ItemForm`, `ItemPage`) and enabled routes in `src/router/index.js`.
- DONE (entity): migrated `Processes` views (`ItemsList`, `ItemForm`, `ItemPage`, `ItemCard`, `BreakTimeItem`, `WorkDateItem`, `FaultItem`).
- DONE (entity): completed `Brands` from `vue2_project/src/views/Brands` with `ItemsList`, `ItemForm`, `ItemPage`, plus enabled routes and sidebar menu entry.
- DONE (entity): completed `Parts` from `vue2_project/src/views/Parts` with `ItemsList`, `ItemForm`, `ItemPage`, plus enabled routes and sidebar menu entry.
- DONE (entity): completed `EquipmentTypesCategories` from `vue2_project/src/views/EquipmentTypesCategories` with `ItemsList`, `ItemForm`, `ItemPage`, plus enabled routes; sidebar menu intentionally deferred until parent `item_types` menu is migrated.
- DONE (entity): completed `Teams` from `vue2_project/src/views/Teams` with `ItemsList`, `ItemForm`, `ItemPage`, plus enabled routes; sidebar menu intentionally deferred until parent `Users` menu is migrated.
- DONE (entity): completed `PlantsVendors` from `vue2_project/src/views/PlantsVendors` with `ItemsList`, `ItemForm`, `ItemPage`, plus added routes and enabled sidebar menu entry.
- DONE (single-file): refactored `src/views/PlantsVendors/ItemForm.vue` to use `useItemForm`, `useRequestsList`, and `localSetupPage` instead of manual setup/loading logic.
- DONE (foundation): added `src/config/entities.js` as entity registry and migrated `Brands` list/page to use it as the first single-source-of-truth example for API/route config.
- DONE (rollout): applied entity registry to the remaining migrated view entrypoints: `Applications`, `Parts`, `Plants`, `PlantsVendors`, `EquipmentTypesCategories`, `Teams`, `Processes`, `Machines`, and `MaintenanceCategories`.
- DONE (pattern): added `createGetRequest()` helper and moved related request URLs for `src/views/PlantsVendors/ItemForm.vue` into `src/config/entities.js`.
- DONE (cleanup): moved `createGetRequest()` into shared `src/api/request_factories.js` so `src/config/entities.js` stays declarative.
- DONE (rollout): applied `createGetRequest()` + registry-backed related URLs to the remaining migrated forms with auxiliary fetches: `Applications`, `Parts`, `Teams`, `Plants`, and `Processes`.
- DONE (cleanup): removed duplicated `plants` related endpoint from `PlantsVendors`; `src/views/PlantsVendors/ItemForm.vue` now references `ENTITIES.Plants` directly for plant list requests.
- DONE (cleanup): removed duplicated `equipmentTypes` related endpoint from `PlantsVendors`; `src/views/PlantsVendors/ItemForm.vue` now references `ENTITIES.EquipmentTypes` directly for equipment-type list requests.
- DONE (cleanup): removed all remaining `createGetRequest(...related...)` usages where the target is already a first-class entity config; migrated forms now reference those entity configs directly.
- DONE (single-file): fixed `src/composables/mixins/useItemPage.js` so explicit `/new` routes work without `route.params.id` and fallback navigation uses existing `NotFound` route.
- DONE (single-file): added `src/views/Companies/ItemsList.vue` in Vue3 style; deferred legacy `Info` table action until `Companies/InfoPage.vue` is migrated and route is enabled.
- DONE (single-file): added `src/views/Companies/ItemForm.vue` in Vue3 style with main/menu/SSO tabs and direct SSO host generation request.
- DONE (single-file): added `src/views/Companies/ItemPage.vue` in Vue3 style with tabbed page wrapper and multipart save flow for SSO certificate uploads.
- DONE (single-file): added `src/views/Companies/InfoPage.vue` in Vue3 style; route enablement and Companies list `Info` action wiring remain separate next steps.
- DONE (details stack): completed `BrandModels` details flow with `MoveForm`, `LocationList`, `DetailsPage`, route wiring in `src/router/index.js`, and supporting registry/runtime fixes.
- DONE (sync): applied latest `vue2_project/src/components/table/CustomDataListTable.vue` selection behavior update to `src/components/table/CustomDataListTable.vue`.
- DONE (sync): applied latest confirmed legacy changes to migrated files: `src/components/table/Row.vue`, `src/components/table/TableHeader.vue`, `src/constants/date_time.js`, `src/helpers/index.js`, `src/views/EquipmentTypes/AnalysisItem.vue`, `src/views/EquipmentTypes/VibrationAnalysisItemsBlock.vue`, and `src/views/Machines/ItemForm.vue`; skipped `src/constants/global.js` as whitespace-only and `src/composables/mixins/useItemsData.js` as already aligned.
- DONE (sync): applied remaining non-SCSS/non-localization legacy updates to migrated files: removed unused `src/helpers/eventLogs.js`, cleaned disabled requisition notification refs from `src/views/Users/ItemForm.vue`, and aligned Sensor chart factory `Chart.js`, `methods.js`, and `enums.js`.
- DONE (single-file): added `src/views/Distributors/ItemsList.vue` in Vue3 style and registered `Distributors` in `src/config/entities.js`.
- DONE (coupled step): added `src/views/Distributors/ItemForm.vue` and `src/views/Distributors/LocationItem.vue` in Vue3 style.
- DONE (single-file): added `src/views/Distributors/ItemPage.vue` in Vue3 style.
- DONE (coupled step): enabled Distributors routes in `src/router/index.js` and sidebar menu entry in `src/constants/menuItems.js`.
- DONE (single-file): added `src/views/TaskProcedures/ItemsList.vue` in Vue3 style and registered `TaskProcedures` in `src/config/entities.js`.
- DONE (coupled step): added `src/views/TaskProcedures/ItemForm.vue`, `PartItem.vue`, and `ProcessItem.vue` in Vue3 style.
- DONE (single-file): added `src/views/TaskProcedures/ItemPage.vue` in Vue3 style.
- DONE (coupled step): enabled TaskProcedures routes in `src/router/index.js` and sidebar menu entry in `src/constants/menuItems.js`.
- DONE (entity): completed `MaintenanceCategories` alignment and migration with shared `useItemsData` list actions, added `ItemForm`, `ItemPage`, and enabled routes/menu.
- DONE (single-file): added `src/views/Controllers/ItemsList.vue` in Vue3 style with shared list flow and controller-specific actions.
- DONE (single-file): added `src/views/Controllers/ItemPage.vue` in Vue3 style with `useItemPage`, tabs, controller-type form selection, and multipart payload detection.
- DONE (single-file): added `src/views/Controllers/ItemForm.vue` in Vue3 style for PDM/Banner controllers; formulas/commands tabs remain lazy dependencies for next steps.
- DONE (single-file): added `src/views/Controllers/FormulasRow.vue` in Vue3 style for PDM/Banner formulas rows.
- DONE (single-file): added `src/views/Controllers/DXMCommandsTab.vue` in Vue3 style for PDM/Banner commands tab.
- DONE (single-file): added `src/views/Controllers/CommandItem.vue` in Vue3 style for DXM command cards.
- DONE (single-file): added `src/views/Controllers/CommandsHistoryItem.vue` in Vue3 style for DXM command history rows.
- DONE (single-file): added `src/views/Controllers/ItemFormUltraSound.vue` in Vue3 style for LubeMatrix controllers.
- DONE (single-file): added `src/views/Controllers/ItemFormCounter.vue` in Vue3 style for OEE controllers.
- DONE (single-file): added `src/views/Controllers/ItemFormNCD.vue` in Vue3 style for NCD controllers.
- DONE (single-file): added `src/views/Controllers/ItemFormUltraSoundWhiteRiver.vue` in Vue3 style for the legacy White River controller form.
- DONE (coupled step): enabled Controllers routes in `src/router/index.js` and sidebar menu entry in `src/constants/menuItems.js`.
- DONE (bugfix): fixed Controllers create blank state by adding a default PDM/Banner type fallback in `src/views/Controllers/ItemPage.vue` and gating formulas tab rendering in `src/views/Controllers/ItemForm.vue`.
- DONE (single-file): wired Controllers devices tab in `src/views/Controllers/ItemForm.vue` to the migrated `src/views/Sensors/BannerSensorsList.vue` with controller-scoped filters; `npm run build` passes.
- DONE (foundation): started `Sensors` migration with `src/composables/useSensors.js`, `SensorsStore` filter/state actions, shared `set_filters`, and dynamic view component loading support.
- DONE (Sensors lists): added Vue3 `ItemsList.vue`, `NCDSensorsList.vue`, `BannerSensorsList.vue`, `FFTRequestBlock.vue`, and table helper cells/actions.
- DONE (Sensors forms/page): added Vue3 `ItemPage.vue` plus base `sensorForm` files: `ItemForm.vue`, `ItemFormNCD.vue`, `ItemFormUltraSound.vue`, `ItemFormWrapper.vue`, `BannerSensorItemWrapper.vue`, `RunningThresholdItem.vue`, and `SubTypeParameterItem.vue`.
- DONE (Sensors thresholds): added Vue3 `ThresholdPeriodItem.vue`, `LevelZoneForm.vue`, `LevelZoneFormWrapper.vue`, and empty legacy-compatible `ReportBlock.vue`.
- DONE (Sensors routes/menu): enabled `/sensors`, `/sensors/new`, `/sensors/:id`, `/sensors/ncd` routes and Sensors sidebar menu entry.
- DONE (Sensors final stage): migrated statistics/charts stack including `StatisticsPage.vue`, `FFTStatisticsPage.vue`, `MultiViewStatisticsPage.vue`, `OneChartPage.vue`, `AnalysisFFT/**`, chart list/item wrappers, FFT chart wrappers, chart support dialogs, and remaining filter blocks.
- DONE (Sensors routes): enabled `/sensors/:id/fft`, `/sensors/:id/stats`, `/sensors/:id/multiview`, and `/sensors/:id/chart`.
- DONE (Sensors follow-up): re-aligned `src/views/Sensors/FilterBlock/PDFandFFTrequestsBlock.vue` and `FFTRequestBlock.vue` with the Vue2 original, restoring PDF websocket/download handling, compare export, grouped FFT/RPM controls, FFT request/last/unlock actions, BannerFilterBlock unlock forwarding, and missing `useSensors` helpers; `npm run build` and targeted `git diff --check` pass.
- DONE (section): migrated `vue2_project/src/views/Maintenance` into `src/views/Maintenance` for the current Vue3 compile scope, including logs, work orders, dashboard, modal wrapper, details/create pages, import page, support cells/previews, `useMaintenance`, route/menu enablement, and `MaintenanceListWrapper` integration.
- DONE (coupled Work Orders Import parity fix): restored the selected-plant guard, shared upload/start/revert/progress container, and drag-and-drop work-order column mapping in `src/views/Maintenance/WorkOrdersImport`; extended the shared import form/container to accept the `useMaintenance` upload action directly; targeted ESLint, targeted `git diff --check`, and the Node 24 production build pass.
- DONE (hardening): expanded `src/views/Maintenance/WorkOrders/ItemForm.vue` toward legacy behavior with attachments/images, parts, snooze, recurring period fields, task procedure selection, `users_ids`, main-instance validation, and multipart payload handling.
- DONE (hardening): expanded `src/views/Maintenance/Logs/ItemForm.vue` toward legacy behavior with total/start-finish time handling, attachments/images, supervisor/sanitization/acknowledge/shift flags, breakdown type, main-instance validation, and multipart payload handling.
- DONE (single-file): hardened `src/views/Maintenance/WorkOrders/ItemsList.vue` toward legacy behavior with production line/machine/asset/equipment filters, category "Without Category" filter option, filtered status options, plant guard for create/export, closer legacy table columns, details/preview actions, and Vue3 async-select settings for asset/equipment lookup.
- DONE (single-file): hardened `src/views/Maintenance/Logs/ItemsList.vue` toward legacy behavior with production line/machine/asset/equipment filters, plant/date guard for export, closer legacy table columns, file and parent work-order actions, log/parent preview modals, and Vue3 async-select settings for asset/equipment lookup.
- DONE (section): migrated `vue2_project/src/views/WorkOrderRequests` into Vue3 with `ItemsList.vue`, `ItemForm.vue`, `ConvertForm.vue`, and `ItemDetailsPreview.vue`; added Work Order Requests entity config, maintenance composable request actions, `/maintenance-requests` route, and sidebar menu entry.
- DONE (section): migrated `vue2_project/src/views/StoreRooms` into Vue3 with `ItemsList.vue`, `ItemForm.vue`, `ItemPage.vue`, and `LocationItem.vue`; enabled `/store-rooms`, `/store-rooms/new`, `/store-rooms/:id`, `/store-rooms/:id/items` routes and sidebar menu entry.
- DONE (section): migrated `vue2_project/src/views/Library/ItemsList.vue` into Vue3 with `useLibrary` / `useItemsData`, Library entity config, corrected filter persistence, `/library` route, and Asset Management sidebar entry; targeted ESLint, targeted `git diff --check`, and the Node 24 production build pass.
- DONE (section): migrated `vue2_project/src/views/Requisitions` into Vue3 with dashboard, list, item page, requisition form, details/action forms, counters, ROI calculator, `usePlantRequisitions`, entity config, `/requisitions`, `/requisitions/:id`, `/roi-calculator` routes, and sidebar menu entries.
- DONE (section): migrated `vue2_project/src/views/RFQS` into Vue3 with `ItemsList.vue`, `ItemForm.vue`, `ItemPage.vue`, `useRfqs`, entity config, `/rfqs`, `/rfqs/new`, `/rfqs/:id` routes, and sidebar menu entry.
- DONE (section): migrated `vue2_project/src/views/ProductionLines` into Vue3 with list/grid, item card, form, item page, utility wrapper, details page, attachment/character subitems, `useProductionLines`, store filter extensions, dashboard production-lines/utilities routes, and production-line create/edit/details routes.
- DONE (runtime alignment): reworked `src/views/ProductionLines/ItemsList.vue` and `src/views/ProductionLines/ItemCard.vue` against the Vue2 original for dropdown/card behavior: restored `View` actions, child list filter propagation, router navigation, deferred dropdown locations loading, and safe item-card navigation through `src/composables/mixins/useItemCard.js`; `npm run build` and targeted `git diff --check` pass.
- DONE (section): migrated `vue2_project/src/views/Machines` into Vue3 with list/grid, item card alignment, form, item page, details page, attachment/character subitems, `useMachines`, machine mock image asset, dashboard machines route reuse, and machine create/edit/details routes.
- DONE (section): migrated `vue2_project/src/views/Assets` into Vue3 with list/grid, item card, form, item page, details page, attachment/composed subitems, `useAssets`, Assets store statistics filters, dashboard assets route, and asset create/edit/details routes.
- DONE (coupled section): re-migrated `src/views/Dashboard/Dashboard.vue` and `src/views/Plants/Details/DetailsPage.vue` toward the legacy parent/child dashboard architecture: Dashboard owns plant loading and passes `plantItem`; Details renders PDM health, counters, embedded EquipmentsLayout/Assets/Machines/ProductionLines/Utilities lists, Maintenance tabs, and fixed Work Order modal loader; `npm run build` and targeted `git diff --check` pass.
- DONE (section): migrated `vue2_project/src/views/Settings` into Vue3, including Settings shell navigation, Faults/NCD Faults, Custom Formulas, Back-End Register Writing, Bearings, Lube Types, Industrial Services, Statistics export, Banner V2 Subtypes, Settings Import, `useSettings`, entity configs, active routes, and Settings sidebar menu entry.
- DONE (section): migrated `vue2_project/src/views/SuccessDashboard` into Vue3 with Success Dashboard container, MainDashboard, MeetingTracker, ROIOnePager, ROIAnalysis placeholder files, shared common components, `useSuccessDashboard`, entity configs, routes, and Customer Success sidebar menu entry; `npm run build` and `git diff --check` pass.
- DONE (section): migrated `vue2_project/src/views/CorporateDashboard` into Vue3 with CorporateDashboard container, CorporateMain, PlantDetailsItem, `/corporate/main` route, Corporate View menu entry, `view_corporate` permission mapping, Pinia/global filters integration, and reused migrated PDM/counters/ROI dashboard blocks; `npm run build` and targeted `git diff --check` pass.
- DONE (coupled step): added first Vue3 `Equipments` layout/list migration with `src/views/Equipments/EquipmentsLayout.vue` and `src/views/Equipments/ItemsList.vue`; layout follows the Vue2 `requestsToDoList`/`initiateRequestsToDoList` dropdown pattern, nested list uses `/equipments/dashboard` with `prepareEquipmentsList`, and `npm run build` passes. Card/details/form substack remains pending.
- DONE (coupled step): added first Vue3 `Equipments/Card` stack with `ItemCard.vue`, `CardSensorItem.vue`, `CardMultiViewItem.vue`, and `TypeOptionBlock.vue`; `Equipments/ItemsList.vue` now renders grid mode through `ItemsGridContainer` and `componentFileLoader`; `npm run build` passes.
- DONE (coupled step): added and later expanded Vue3 `Equipments` create/edit scope with `ItemPage.vue`, `ItemFormWrapper.vue`, and `ItemForm.vue`; code-level recheck confirms RPM controls, vibration analysis rules, child component/type-related submit, PDM sensor tab wiring, sensor detach flow, and Multi View tab submit are present; runtime smoke-test was not performed.
- DONE (route step): enabled the migrated Equipments dashboard list route in `src/router/index.js` at `/dashboard/equipments`; Vue2 create/edit routes were commented and details components are still pending, so those routes remain inactive.
- DONE (coupled step): migrated the dashboard multiform modal from Vue2 into `src/views/Dashboard/MultiFormWrapper.vue` and `MultiFormItemWrapper.vue`; `DynamicFormContainer` uses explicit `componentFileLoader` / `formComponentFileLoader`, and `ProductionLines`, `Machines`, `Assets`, and `Equipments` list create/edit actions now open `Dashboard/MultiFormWrapper`. Machine/Asset/Equipment forms now consume multiform parent item data for `production_line_id`, `machine_id`, and `asset_id`; `npm run build` passes.
- DONE (runtime fix): Equipment editing inside `MultiFormItemWrapper` now fetches the full `/equipments/:id` item before rendering `Equipments/ItemFormWrapper`, matching the deeper form-data needs of Equipments compared with dashboard list rows; `npm run build` passes.
- DONE (section): migrated `vue2_project/src/views/Equipments/Details` into Vue3 with `DetailsPage.vue`, `EquipmentInfoBlock.vue`, `QuoteTab.vue`, crossover/analogues tables, move history, PDM buttons, `equipment_mock.jpg`, nested `/equipments/:id/details/*` routes, and Sensors stats route-param compatibility; `npm run build` and targeted `git diff --check` pass.
- DONE (follow-up): re-applied Vue2 `itemCardMixin` behavior across migrated ItemCard files through `src/composables/mixins/useItemCard.js`; Assets/Machines/ProductionLines/BrandModels title clicks now use the shared flow with Pinia page reset callbacks, Processes restores preview overlay handling, and `npm run build` plus targeted `git diff --check` pass.
- DONE (single-file follow-up): re-checked `src/views/Maintenance/WorkOrders/ItemForm.vue` against Vue2 and restored missing form behavior: `showJustInfo`, filtered statuses, task procedure info/dialog, technician users/teams tabs, create Part/WO Type modal callbacks, legacy snooze-change UI, equipment card grouping, and recurring period-date submit normalization; `npm run build` and targeted `git diff --check` pass.
- DONE (single-file performance): updated `src/components/common/Datepicker.vue` to lazy-mount Element Plus `el-date-picker` after first interaction, using a lightweight div placeholder styled like the datepicker input with icons from `@element-plus/icons-vue` and reducing initial eager calendar table DOM on pages with many datepickers; `npm run build` and targeted `git diff --check` pass.
- DONE (single-file follow-up): re-aligned `src/views/SuccessDashboard/MeetingTracker/ItemForm.vue` data loading with the Vue2 `requestsToDoList` pattern by replacing manual `Promise.all` loaders with Vue3 `useRequestsList`, plant-bound request settings, and delayed initiation after the plant/id guard; `npm run build` and targeted `git diff --check` pass.
- DONE (single-file follow-up): re-aligned `src/views/SuccessDashboard/DetailsPage.vue` requests with Vue2-style `doFetchAction` through `useRequestsList`, and centralized child route props so `plantItem`, `sensorsList`, `sensorsLoading`, and `preventSetNavbar` are consistently passed to child components; `npm run build` and targeted `git diff --check` pass.
- DONE (coupled runtime fix): restored `src/views/SuccessDashboard/common/SensorsAlarmsContainer.vue`, `SensorAlarmsChartsListWrapper.vue`, `SensorAlarmsChartItemContainer.vue`, and `MeetingTracker/ItemForm.vue` toward the Vue2 Sensors alarms modal flow so alarm graphs are grouped by sensor, built with `sensorAlarmsChartsListFactory` / `SensorAlarmsChart`, receive `measurement/additionalProps`, and no longer bubble `handleChartContainerReady` / `chartLoadEvent` warnings to the form; `npm run build` and targeted `git diff --check` pass.
- DONE (single-file follow-up): changed `src/views/SuccessDashboard/MeetingTracker/ItemForm.vue` `fetchAlarms` to use `useRequestsList().doFetchAction` through `methodsMap.fetch_alarms` and removed direct async loading / console logging; `npm run build` and targeted `git diff --check` pass.
- DONE (single-file follow-up): fixed `src/views/SuccessDashboard/MeetingTracker/ItemForm.vue` alarm fetch trigger after the `useRequestsList` migration by calling `fetchAlarms(graphsPeriod)` once initial list requests finish and `graphsPeriod` already exists; `npm run build` and targeted `git diff --check` pass.
- DONE (single-file runtime fix): fixed `src/views/SuccessDashboard/common/SensorAlarmsChartsListWrapper.vue` render crash by removing `Object.freeze` from reactive `sensorData` / `equipmentData`; `npm run build` and targeted `git diff --check` pass.
- DONE (single-file runtime fix): fixed `src/views/SuccessDashboard/common/SensorAlarmsChartItemContainer.vue` Highcharts `missingModuleFor=flags` / `missingModuleFor=gauge` runtime errors by rendering alarm charts as `stockChart` with initialized `highcharts-more`, `stock`, `boost`, and `annotations` modules; `npm run build` passes.
- DONE (single-file runtime fix): fixed `src/views/SuccessDashboard/MainDashboard/GaugeStatisticsContainer.vue` Highcharts `missingModuleFor=gauge` runtime error by passing `additionalProps.hcInstance` with initialized `highcharts-more`, `boost`, and `annotations`; `npm run build` passes.
- DONE (single-file runtime fix): fixed `src/views/SuccessDashboard/MeetingTracker/ItemForm.vue` unresolved `/static/img/no_logo.jpg` import by adding `src/assets/img/no_logo.jpg` and using a Vite asset import; `npm run build` passes.
- DONE (coupled parity fix): restored `src/views/SuccessDashboard/common/SensorAlarmsChartItemContainer.vue`, `NotesItem.vue`, and `ChartCommentForm.vue` toward Vue2 behavior for chart toggle, selection/annotation comments, notes UI, and `handleValidateNotesItems`; `npm run build` passes.
- DONE (single-file parity fix): restored `src/views/SuccessDashboard/common/SensorsAlarmsContainer.vue` `enableDragNDrop` behavior with lock/unlock UI, `useDragNdropSortable`, reorder history, save-time graph sorting, and `newNotesSettings`; `npm run build` passes.
- DONE (single-file warning fix): updated `src/views/SuccessDashboard/common/NotesItem.vue` checkboxes to Element Plus `true-value` / `false-value` API; `npm run build` passes.
- DONE (coupled drag/drop fix): restored `src/views/SuccessDashboard/common/SensorAlarmsChartsListWrapper.vue` dark overlay markup and scoped `src/views/SuccessDashboard/common/SensorsAlarmsContainer.vue` sortable wrapper selector to `.sensors-alarms-drag-n-drop-wrapper`; `npm run build` passes.
- DONE (single-file navigation fix): fixed `src/components/common/ButtonsNavbar.vue` active class handling for nested route paths used by `src/views/SuccessDashboard/DetailsPage.vue`; `npm run build` passes.
- DONE (coupled navigation fix): forwarded `preventSetNavbar` from Success Dashboard `DetailsPage.vue` into Meeting Tracker and ROI One Pager item pages so local `ButtonsNavbar` route switches do not replace parent navbar settings; `npm run build` passes.
- DONE (coupled parity fix): rebuilt `src/views/SuccessDashboard/ROIOnePager/ItemForm.vue` and `DynamicFormItem.vue` toward the Vue2 ROI One Pager contract, including Form/File tabs, readonly/details mode, issues/cost-avoidance rows, uploads, PDM graphs, history dialog, submit payload cleanup, and exposed `SensorsAlarmsContainer.saveSensorsAlarmsChartsForms`; `npm run build` passes.
- DONE (single-file request-pattern fix): changed `src/views/SuccessDashboard/ROIOnePager/ItemsList.vue` production-line and machine option loading from manual async functions to Vue3 `useRequestsList` / `requestsToDoList` bound requests; `npm run build` passes.
- DONE (single-file request-pattern fix): changed `src/views/SuccessDashboard/ROIOnePager/ItemForm.vue` so `fetch_assets` uses `useRequestsList` / `requestsToDoList`, while `fetchAsset` and `fetchAssetSensors` use `doFetchAction`; `npm run build` passes.
- DONE (single-file runtime fix): fixed `src/views/SuccessDashboard/ROIOnePager/DynamicFormItem.vue` Element Plus button icon usage by moving `icomoon icon-plus` into child `<i>` elements; `npm run build` passes.
- DONE (single-file runtime fix): guarded `src/views/SuccessDashboard/common/SensorAlarmsChartsListWrapper.vue` against alarm graph groups without a valid chart settings key or parameters, preventing `charts_configs for "undefined" not found`; `npm run build` passes.
- DONE (single-file runtime fix): fixed `src/views/CorporateDashboard/CorporateDashboard.vue` startup by using `initHighchartsModule` for Highcharts modules and passing mutable navbar settings into the global store; `npm run build` passes.
- DONE (coupled runtime fix): restored `plantsList` / `allPlantsRoiStatistics` propagation from `src/views/CorporateDashboard/Details/PlantDetailsItem.vue` through `src/views/SuccessDashboard/MainDashboard/ROIStatisticsContainer.vue`, preventing corporate all-plants ROI gauge from requesting bare `/api`; `npm run build` passes.
- DONE (single-file parity fix): restored `disableTabs` behavior in `src/views/SuccessDashboard/MainDashboard/ROIStatisticsContainer.vue` for Corporate Dashboard, hiding lower production-line/machine/asset tab charts and enabling gauge `statistics-outside`; `npm run build` passes.
- DONE (single-file runtime fix): fixed `src/views/Sensors/SensorFFTRequestButton.vue` render crash on Sensors statistics pages by treating `is_fft_processing` as a boolean and supporting both `propsData` and `itemData`; `npm run build` passes.
- DONE (coupled runtime/parity fix): restored Vue2 Highcharts flag SVG assets in `public/static/img/icons`, fixing `shape: "url(/static/img/icons/success_flag.svg)"`; the manual flag tooltip formatting and hover refresh handlers in `src/modules/charts_factory/enums/index.js` were later rolled back at user request; `npm run build` passes.
- DONE (single-file parity fix): restored `src/views/Sensors/PossibleProblemsBlock.vue` watcher behavior to Vue2 parity by using separate non-immediate watchers for `sensorParamsForSetupProblems` and `daterange` instead of a combined deep/immediate watcher; `npm run build` passes.
- DONE (coupled parity fix): rechecked `src/views/Sensors/charts` against `vue2_project` and restored `ChartItemContainer.vue` one-chart legend, compare header classes, hidden-chart lazy fetch, disable button initialization, show-history zoom reset via exposed `ChartZoom.vue` methods, plus `HeaderRightPart.vue` export payload fields `metricSystemType` and `X-Timezone-Offset`; `npm run build` passes.
- DONE (single-file parity fix): restored `src/views/BrandModels/ItemsList.vue` storeroom grid/list behavior with the Vue2 grid switcher, `ItemsGridContainer`, equipment type card props, QTY/details navigation, original `activeGrid` store-based calculation, and `preventSetNavbar` forwarding into `useItemsData`; `npm run build` passes.
- DONE (single-file runtime fix): fixed `src/views/Plants/Details/DetailsPage.vue` navbar setup to store `navbarSettings.value` instead of the readonly computed ref, preventing readonly `navbarSettings` warnings when switching embedded Asset/Storeroom lists; `npm run build` passes.
- DONE (FFT section parity): re-aligned `src/views/Sensors/FFTStatisticsPage.vue` and `src/views/Sensors/charts/fft` against Vue2 for the current compile scope, including legacy FFT page header/controls, prev/next FFT route flow, hidden-chart map rebuild, FFT chart cursor/peaks/periodic/waveform/RPM cursor behavior, vibration-analysis rule override save/delete actions in `useSensors`, waterfall 3D controls, and `/ncd/:id/fft/:fftId` plus `/banner/:id/fft/:fftId` routes; `npm run build` and targeted `git diff --check` pass.
- DONE (Vue2 sync): migrated latest Vue2 changes into already migrated Vue3 files: chart factory error event propagation, Sensors chart `window.graphRenderError`, Equipment sensor detach endpoint, Banner sensor initial setup watcher, LubeMatrix V4 Banner submit type; Vue3 FFT statistics API base URL was already aligned; `npm run build` and targeted `git diff --check` pass.
- DONE (Vue2 sync): migrated latest Requisitions/constants/helper changes into already migrated Vue3 files: `datePickerAdditionalShortcuts2`, `last_6_months` date range, Requisition approve downtime/hour + hours-saved + contractor quote fields and paired validation, print report fields, and updated ROI Calculator flow/list filters/export payload; localization keys were already present in Vue3; `npm run build` and targeted `git diff --check` pass.
- DONE (section): migrated `vue2_project/src/views/Processes/Details` into Vue3 with DetailsPage, DashboardPage, ChartItemContainer, LogsList, EventLogForm, EditBreakTimeForm, `useProcesses`, statistics/downtime filters in `ProcessesStore`, and details/dashboard/logs routes; `npm run build`, targeted `git diff --check`, and migration-rule scan pass.
- DONE (section): migrated `vue2_project/src/views/Settings/Import` into Vue3 with shared Import components, master/plant import flows, settings import tab/history/detail, plant import logs/error rows, TestingStore import/log actions, sidebar/settings navigation, and direct/settings routes; `npm run build`, targeted `git diff --check`, and migration-rule scan pass.
- DONE (section follow-up): re-migrated `src/views/Requisitions/Details` toward Vue2 parity: restored action modal ownership in `WorkOrderDetails.vue`, technical-process rendering in `CompleteFormContainer.vue`, execution-time/material subitem forms via `useSubItem`, complete/save-progress and approve/reset/save flows through existing composables, attachment/multipart handling, DetailsItem array/attachment rendering, and print report structure; `npm run build`, targeted `git diff --check`, and migration-rule scan pass.
- DONE (Vue2 sync): migrated latest Sensors/chart/sub-item legacy changes into already migrated Vue3 files: boolean `setIfEmpty` false normalization in `useSubItemsList`, Sensor chart one-chart navigator/scrollbar disable with x panning, `ChartsListWrapper` one-chart additional-props/header/getParams guards, and Banner M25 running-threshold parameter filtering in Sensor form; skipped the whitespace-only Sensor methods change. WebSocket transport was later re-migrated separately; `npm run build` and targeted `git diff --check` pass.
- DONE (coupled WebSocket step): added `src/services/WebSocketService.js` based on the legacy service with Vite environment adaptation and rebuilt `src/composables/mixins/useWebSocket.js` as its Vue wrapper while preserving the existing Vue3 setup/close/send API; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file WebSocket parity): restored the legacy DXM/lubrication socket-completion flow in `src/views/Sensors/FilterBlock/UltrasoundFilterBlock.vue` through `useWebSocket`, including pre-request subscription, lube-shot/controller/status matching, chart update forwarding, error handling, and socket cleanup; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file WebSocket contract): audited all current `setupWebSocket` consumers and normalized `useWebSocket` channel callbacks to the common `{ type, data }` message contract expected by legacy and Vue3 handlers; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file WebSocket lifecycle): audited current DXM/FFT `onOpen` consumers against Vue2 and changed `useWebSocket` so consumer-ready callbacks run only after successful private-channel subscription, while `localHandleConnected` remains transport-level; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file WebSocket consumer audit): migrated the last direct consumer in `src/views/Sensors/ItemPage.vue` to `useWebSocket` for `NCD.COMMAND` save-status handling; native WebSocket construction now remains only inside `WebSocketService`; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file runtime fix): fixed first-login auto-logout initialization in `src/components/layout/DashboardLayout.vue` by reacting to asynchronous `authUser` population and guarding against duplicate idle timers; existing timeout comments/settings remain unchanged; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file runtime fix): fixed logout route access in `src/composables/mixins/useNavigation.js` by obtaining `useRoute()` during setup and reading the current `fullPath` per navigation call, preventing undefined/stale routes from delayed auto-logout; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file router parity): restored the legacy query-token authentication and global `beforeEachHook` flow in `src/router/index.js`, removed forbidden-route persistence on `limited_access`, and retained the corrected Vue3 MFA early return; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (single-file runtime fix): fixed `src/components/pages/NewPasswordForm.vue` 422 handling by extracting Axios status with response fallback and replacing invalid `Lang.t` notification calls with `tt`; targeted ESLint, `git diff --check`, and `npm run build` pass.
- DONE (Vue2 sync restore): restored the migrated legacy delta from commit `550d339` after `src/` was overwritten: Assets/Machines/ProductionLines navbar/form changes and the Controllers `CMD0200` WebSocket bypass are present again. Plants already retained the `is_blocked` removal, localization values already matched, and Utilities details remains skipped because it is not migrated; targeted ESLint, targeted `git diff --check`, and `npm run build` pass.
- DONE (charts_factory parity): fixed selected Sensor chart factory differences in Vue3: added `UNIT_TYPES.ULTRASONIC_G` / `constants.usg`, mapped Banner M25 ultrasound RMS/peak to `ULTRASONIC_G`, changed Banner V2.1 literal short names to localization keys, and returned `USg` for Banner M25 FFT X waveform / transformed acceleration / X acceleration; other reported chart factory differences remain unchanged; `npm run build` and targeted `git diff --check` pass.
- DONE (charts_factory parity): restored Vue2-style FFT RPM cursor drag/update behavior in `src/modules/charts_factory/controllers/Sensor/classes/FFTChart.js`, including `isUpdateOnly`, `serieId` / `pointId`, point title updates, drop-time options sync, and factory-level cursor synchronization; `npm run build` and targeted `git diff --check` pass.
- DONE (charts_factory parity): restored Vue2-style FFT analysis annotation value fallback in `src/modules/charts_factory/controllers/Sensor/classes/FFTChart.js`, using `option_value.value` or `custom_value` for custom rules; `npm run build` and targeted `git diff --check` pass.
- DONE (charts_factory parity): initialized `this.requestsList = []` in `src/modules/charts_factory/classes/StatisticsTransformator.js` base constructor to match Vue2 and avoid early `setupDataAccessor()` crashes before request injection; `npm run build` and targeted `git diff --check` pass.
- DONE (FFT runtime fix): restored draggable FFT annotations by passing `additionalProps.hcInstance` from `src/views/Sensors/FFTStatisticsPage.vue` into `ChartWrapper` in `src/views/Sensors/charts/fft/FFTChartItemContainer.vue`, so the annotations/draggable-points modules initialized for the FFT page are used by the rendered chart; `npm run build` and targeted `git diff --check` pass.
- DONE (Sensors chart runtime fix): guarded `src/modules/charts_factory/controllers/Sensor/classes/Chart.js` history-series detection against numeric/non-string Highcharts series ids, fixing Banner grouped-by-axis chart mount crashes; `npm run build` and targeted `git diff --check` pass.
- DONE (Vue2 sync): migrated latest Machine/Production Line silence mode fields and submit normalization, Plant `is_blocked` switch, and Requisitions Hours fields from `actual_time` to `execution_total_time`; Vue3 localization already had the added silence mode phrase keys; `npm run build` and targeted `git diff --check` pass.
- DONE (Vue2 sync, 2026-08-04): migrated the latest six-file legacy delta into existing Vue3 counterparts: Multi View Alarms constants/localization, Requisition MaterialItem prop cleanup, Banner M25 running-threshold restriction for available and loaded rows, and Meeting Tracker PDF form initial `plant_id`; targeted ESLint, targeted `git diff --check`, and the Node 24 production build pass.
- DONE (Details runtime fix): normalized `chartLegendEvents` placeholders from Object to Array in Asset, Machine, and Production Line Details pages, removing the `ItemPDMsStatisticBlock` prop warning and chart factory `eventsList.forEach` error; targeted ESLint, targeted `git diff --check`, and the Node 24 production build pass.
- DONE (FileUploadBlock runtime fix): replaced the old `fileUploadBlockRef` registration with `setSubItemRef` in Machine form and the only other matching component, ROI One Pager form; project-wide scan confirms no remaining overlap, and targeted ESLint, `git diff --check`, and the Node 24 production build pass.
- Current next focus: runtime smoke-test SuccessDashboard Meeting Tracker / ROI One Pager with real item data; code-level form file set has been rechecked against Vue2.
- Current next focus: runtime smoke-test Sensors flows with real data; code-level chart/form parity follow-ups have been applied, with legacy `sensorForm/ItemFormPump.vue` logic represented in the migrated ultrasound form stack rather than as a separate file.
- DONE (build): fixed the existing non-Sensors build blocker and follow-up compile blockers; `npm run build` now passes.
- Current next focus: do not reopen completed `BrandModels` details work unless a new issue is reported.

- useCreateFormItem: when migrating components, pass list as ref/array (string prop is not supported).
- useDashboardListsReorder: pass tt, emit, itemsList, reorderAction, filters, itemsLoading, globalFilters, fromDetailsPage, showEditModal.
- useDragNdropDroppable: pass wrapperSelector, containersQuantity, dragStartCallback, dropValidateCallback, dropCallback.
- useDragNdropSortable: pass wrapperSelector, draggingLockedProp, reorderHandler, dragStartHandler.
- useExportListToFile: pass prepareFilters if custom; otherwise defaults to prepareRangeParams.
- fetchItemsHelper: use useFetchAction instead.
- itemsDataMixin: core covered by useItemsData; UI helpers migrate with components.
- useItemForm: pass formData/itemData refs and submitAction function.
- useItemPage: pass itemFormRef, saveItem, changeRoute, itemData.
- useLoadMore: use loadMore(() => ...) and pass dataList as ref/array to prepareLoadMorePayload.
- useMainInstanceDetailsPage: pass itemData, instanceDataKey, instanceViewName, itemsName, predefinedFilters, filters, initialPageSetup, maintenanceListWrapperRef, equipmentsLayoutRef.
- useMultiform: pass emit, formData, instancesItemsData, multiFormFilters.
- useSelectFile: pass formData (ref/obj) and clearValidate.
- useRequestsList: update bindTo config to getValue()/alternateGetValue() functions (no string paths).
- useSensorType: pass currentSensorTypeData (ref/obj).
- useSubItem: pass itemData, formData, itemFormRef, emit, and optional hooks.
- useSubItemsList: pass formData and refsMap (object of template refs); lists should be ref/array (no string prop).
- useSwitchGridView: pass filters ref, setFilters function, and preventFetch ref if needed.
- useTabs: pass tabsList (ref/array), optional hideTabsBar, switchTabTo, localTabSetup.
- useWebSocket: pass socketChannel, onMessage callback, and optional socketReadyRef; use readyMap if no ref.
- useImport: pass emit, currentLogId ref, successImportCallback, and actions as functions (progressAction/revertAction).
- useInitPageData: pass fetchItem function, itemsName, and optional additionalNavbarSettings; itemData/loadContent/itemLoading refs if you need control.
- Consider optional wrapper: `useItemPageWithInit` that composes useItemPage + useInitPageData after migration.
- useItemCard: pass cardData, changeRoute, titleLinkRoute, resetPageFiltersList, emit.
